Hello $sharedUser.lastName, $sharedUser.firstName

#if($sharedTags.size() == 1)
Specimen list by title "$sharedTags.get(0).label" has been shared to you by $user.lastName, $user.firstName. You can now access the list after you login to caTissue.
#else 
Specimen lists by title #foreach($tag in $sharedTags) #if($velocityCount == $sharedTags.size())"$tag.label"#{else}"$tag.label",#end #end has been shared to you by $user.lastName, $user.firstName. You can now access the lists after you login to caTissue. 
#end 

$footer
