language: java
node_js:
  - stable

before_script:
  - cd www
  - rm -rf /home/travis/.npm/*
  - npm install -g bower
  - npm install
  - bower install -f
  - cd ..

script: gradle build

notifications:
  email:
    recipients:
      - os-v20-devs@krishagni.com
    on_success: always
    on_failure: always


branches:
  only:
    - master
