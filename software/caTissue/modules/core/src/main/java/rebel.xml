<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<application>

    <classpath>
        <dir excludes="**/.svn/" name="${deploy.temp.dir}/WEB-INF/classes"/>
    </classpath>

    <web>
        <link target="/">
            <dir name="${deploy.temp.dir}"/>
        </link>
    </web>
</application>
